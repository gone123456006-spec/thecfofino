# App images

- **ai-icon.png** – AI Assistant / Chat icon (tab bar + chat screen). Add your glossy AI icon and update `constants/assets.ts`.
- **company-registration-icon.png** – Company Registration service card. Use image with transparent background; update `constants/assets.ts` when added.
- **gst-filing-icon.png** – GST Filing service card. Use image with transparent background; update `constants/assets.ts` when added.
