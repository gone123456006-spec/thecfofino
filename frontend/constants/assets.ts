/** Header / login logo (Finovert). */
export const HeaderLogo = require('../assets/images/header-logo.png');

/** Home page navbar logo (Finovert). */
export const NavbarLogo = require('../assets/images/logogogw.png');

export const LogoImage = require('../assets/images/icon.png');

/** AI Assistant / CFO Assistant icon (tab bar + chat screen). */
export const AIIcon = require('../assets/images/AI.png');

/** Company Registration service icon (Our Services). */
export const CompanyRegistrationIcon = require('../assets/images/cr.png');

/** GST Filing service icon (Our Services). */
export const GSTFilingIcon = require('../assets/images/gst-filing.png');

/** ITR Filing service icon (Our Services). */
export const ITRFilingIcon = require('../assets/images/itr.png');

/** CFO Services icon (Our Services). */
export const CFOServicesIcon = require('../assets/images/cfo-services.png');

/** TDS Filing service icon (Our Services). */
export const TDSFilingIcon = require('../assets/images/tds-filing.png');

/** Invoice Financing service icon (Our Services). */
export const InvoiceFinancingIcon = require('../assets/images/invoice-icon.png');
