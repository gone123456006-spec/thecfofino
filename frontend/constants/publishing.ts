/**
 * Store listing identifiers — must stay in sync with `app.json`:
 * - `expo.android.package`
 * - `expo.ios.bundleIdentifier`
 */
export const ANDROID_APPLICATION_ID = 'com.brandovert.finovert';
export const IOS_BUNDLE_IDENTIFIER = 'com.brandovert.finovert';

export const PLAY_STORE_LISTING_URL = `https://play.google.com/store/apps/details?id=${ANDROID_APPLICATION_ID}`;
