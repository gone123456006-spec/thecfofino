#!/usr/bin/env node
/**
 * Finovert Backend — Node.js server entry point.
 * Run: node server.js   or   npm start
 * Requires .env (MONGODB_URI, PORT, JWT_SECRET, etc.)
 */
require('./src/index.js');
